<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enio | Vysledky VDT - 60 minút</title>
    <link rel="icon" type="image/x-icon" href="/img/Enio_symbol_green_transparent_small.ico">
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

</head>
<body>
    <div class="header">
        <div class="logo">
            <img src="/img/Enio_wordmark_black_background.jpg" alt="Logo" style="height: 50px; width: auto;">
        </div>
        <form id="dateForm" class="navigation-form">
            <input type="hidden" name="direction" id="direction" />
            <button type="button" class="triangle-button backward" onclick="navigate('backward')"></button>
            <input type="date" name="currentDay" id="currentDay" value="<%= currentDay %>" />
            <button type="button" class="triangle-button forward" onclick="navigate('forward')"></button>
        </form>

    </div>
    <div class="navigate-line">
        <div id="home-icon" onclick="toggleNav()">
            <img src="/img/Enio-home-icon-small.png" alt="home" style="height: 20px; width: auto;">
        </div>
        <div class="home-text">Výsledky VDT - 60 minút</div>
    </div>

    <%- include('../partials/navigator') %>

    </div>

    <div class="container">
        <div class="left-dva-grafy">
            <div class="chart-dva-vertikalne">
                <canvas id="chart1"></canvas>
            </div>
            <div class="chart-dva-vertikalne">
                <canvas id="chart2"></canvas>
            </div>
        </div>
        <div class="right">
            <h3>Prehľad ceny DT vs ceny VDT-60</h3>
            <table>
                <thead>
                    <tr>
                        <th>Perioda</th>
                        <th>Čas</th>
                        <th>Cena DT</th>
                        <th>Cena VDT min.</th>
                        <th>Cena VDT max.</th>
                        <th>Cena VDT avg.</th>

                    </tr>
                </thead>
                <tbody id="table-body">
                    
                    <% dataJSON.forEach(function(item) { %>
                        <tr>
                            <td class="timestamp"><%= item.perioda %></td>
                            <td class="timestamp"><%= item.utc_cas %></td>

                            <td>
                                <% if (item.cena_SK !== null && item.cena_SK !== undefined) { %>
                                    <%= item.cena_SK.toFixed(2) %>
                                <% } else { %>
                                    N/A
                                <% } %>

                            </td>

                            <td>
                                <% if (item.cena_VDT60_min !== null && item.cena_VDT60_min !== undefined) { %>
                                    <%= item.cena_VDT60_min.toFixed(2) %>
                                <% } else { %>
                                    N/A
                                <% } %>

                            </td>

                            <td>
                                <% if (item.cena_VDT60_max  !== null && item.cena_VDT60_max !== undefined) { %>
                                    <%= item.cena_VDT60_max.toFixed(2) %>
                                <% } else { %>
                                    N/A
                                <% } %>

                            </td>

                            
                            <td>
                                <% if (item.cena_VDT60_avg !== null && item.cena_VDT60_avg !== undefined) { %>
                                    <%= item.cena_VDT60_avg.toFixed(2) %>
                                <% } else { %>
                                    N/A
                                <% } %>

                            </td>

                        </tr>
                    <% }); %>

                </tbody>            
            </table>
        </div>
    </div>

    <script>

        let dataGraf_obj = ' <%-JSON.stringify(dataJSON) %> ';
        let dataGraf = JSON.parse(dataGraf_obj);

        const labels = dataGraf.map(item => item.perioda);
        const DT_SK_cena = dataGraf.map(item => item.cena_SK|| null);
        const VDT60_cena_min = dataGraf.map(item => item.cena_VDT60_min ?? null);
        const VDT60_cena_max = dataGraf.map(item => item.cena_VDT60_max ?? null);
        const VDT60_cena_avg = dataGraf.map(item => item.cena_VDT60_avg ?? null);
        const VDT15_cena_avg = dataGraf.map(item => item.cena_VDT15_avg ?? null);
        const objem_VDT_nakup = dataGraf.map(item => item.objem_VDT_nakup ?? null);
        const objem_VDT_predaj = dataGraf.map(item => item.objem_VDT_predaj * -1 ?? null);

        // Create the chart

       const ctx = document.getElementById('chart1').getContext('2d');
       window.myChart1 = new Chart(ctx, {
           type: 'line',
            data: {
                labels: labels,
                datasets: [
                    
                 {
                    label: 'Cena VTD60 avg',
                    data: VDT60_cena_avg,
                    backgroundColor: 'green',
                    borderColor: 'green',
                    borderWidth: 2
                },                 {
                    label: 'Cena VTD15 avg',
                    data: VDT15_cena_avg,
                    backgroundColor: 'red',
                    borderColor: 'red',
                    borderWidth: 2
                },                {
                    label: 'Cena DT',
                    data: DT_SK_cena,
                    type: 'line',
                    fill: 'origin',
                    stepped:'middle',
                    backgroundColor: 'rgba(217, 217, 217, 0.6)',  
                    borderColor: 'rgba(191, 191, 191, 0.6)',
                    borderWidth: 1,
                    pointRadius: 1
                },
                {
                    label: 'Cena VDT min',
                    data: VDT60_cena_min,
                    type: 'line',
                    backgroundColor:  'rgba(127, 183, 127, 0.3)',
                    borderColor: 'rgba(127, 183, 127, 0.4)',
                    borderWidth: 1
                
                },
                {
                    label: 'Cena VDT max',
                    data: VDT60_cena_max,
                    backgroundColor: 'rgba(127, 183, 127, 0.3)',
                    borderColor: 'rgba(127, 183, 127, 0.4)',
                    borderWidth: 1,
                    fill: '-1'  // Fill between this and the previous dataset
                }

  
            ]},
            options: {
                interaction: {
                    intersect: true,
                    mode: 'index'},
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    title: {
                        display: true,
                        text: 'Prehľad cien ',
                        font: { size: 14 }
                    }
                }
        }});



        const ctx1 = document.getElementById('chart2').getContext('2d');
        window.myChart2 = new Chart(ctx1, {
            type: 'bar',
            data: {
                labels: labels,
                datasets: [
                {
                    label: 'Nákup VDT',
                    data: objem_VDT_nakup,
                    type: 'bar',
                    backgroundColor: 'green',
                    borderColor: 'green',
                    borderWidth: 1
                
                },
                {
                    label: 'Predaj VDT',
                    data: objem_VDT_predaj,
                    type: 'bar',
                    backgroundColor: 'rgba(127, 183, 127, 0.3)',
                    borderColor: 'rgba(127, 183, 127, 0.4)',
                    borderWidth: 1
                
                }
            ]
        },
        options: {
            interaction: {
                intersect: true,
                mode: 'index'
            },
            responsive: true,
            scales: {
                x: {
                    stacked: true
                },
                    y: {
                        stacked: true
                    }
                },
                maintainAspectRatio: false,
                plugins: {
                    title: {
                        display: true,
                        text: 'Zobchodované množstvo ',
                        font: { size: 14 }
                    }
                }
            }});


    </script>

<script src="js/script-vdt-60.js"></script>          <!--script, ktorý je zodpovedný pre navigátora-->

</body>
</html>
